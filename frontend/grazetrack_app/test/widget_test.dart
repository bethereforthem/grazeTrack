// Basic smoke test for GrazeTrack app
// Run with: flutter test

import 'package:flutter_test/flutter_test.dart';

void main() {
  // Note: Full app tests require Firebase to be initialized.
  // Add integration tests after running: flutterfire configure

  test('App constants are defined', () {
    // Placeholder test — the app structure is verified by the build
    expect(true, isTrue);
  });
}
